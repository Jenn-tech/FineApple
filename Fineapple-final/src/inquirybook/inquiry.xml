<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="inquiry">

	<insert id="insert" parameterType="inquirybook.InquiryBookVo">
		INSERT ALL
		INTO inquiry (SERIAL, MSERIAL, MEMBERNAME, PWD, SUBJECT, DOC, DOCVISIBLE, CREATED, INQUIRYTYPE, PSERIAL, HIT)
		VALUES(seq_inquiry.nextval, 0, #{memberName}, #{pwd}, #{subject}, #{doc}, 'no', sysdate, '환불', 0, 0)
		<if test="attList != null">
			<foreach collection="attList" item="att">
				INTO inquiry_photo(serial, pserial, sysfile, orifile)
				VALUES(getInquiryAttSeq(), seq_inquiry.currval, #{att.sysFile}, #{att.oriFile})
			</foreach>
		</if>
		SELECT * FROM dual
	</insert>

	<insert id="insert_att" parameterType="inquirybook.InquiryBookVo" >
		INSERT ALL
		<foreach collection="attList" item="att">
			INTO inquiry_photo(serial, pserial, sysfile, orifile)
			VALUES (getInquiryAttSeq(), ${serial}, #{att.sysFile}, #{att.oriFile})
		</foreach>
		SELECT * FROM dual
	</insert>

	<insert id="insertanswer" parameterType="inquirybook.InquiryBookVo">
		INSERT ALL
		INTO inquiry (SERIAL, MSERIAL, MEMBERNAME, PWD, SUBJECT, DOC, DOCVISIBLE, CREATED, INQUIRYTYPE, PSERIAL, HIT)
		VALUES(seq_inquiry.nextval, 0, #{memberName},'해준', #{subject}, #{doc}, 'no', sysdate, '환불', ${pserial}, 0)
		<if test="attList != null">
			<foreach collection="attList" item="att">
				INTO inquiry_photo(serial, pserial, sysfile, orifile)
				VALUES(getInquiryAttSeq(), seq_inquiry.currval, #{att.sysFile}, #{att.oriFile})
			</foreach>
		</if>
		SELECT * FROM dual
	</insert>
	
	<select id="select" resultType="inquirybook.InquiryBookVo" parameterType="bean.CsPage">
		SELECT * FROM inquiry
	</select>
	
	<select id="tot_list_size" resultType="int" parameterType="bean.CsPage">
		SELECT count(serial) FROM inquiry
		WHERE subject like '%${findStr}%' or doc like '%${findStr}%' 
	</select>
	
	<select id="view" resultType="inquirybook.InquiryBookVo" parameterType="int">
		SELECT * FROM inquiry WHERE serial = #{_parameter}
	</select>
	
	<select id="select_att" resultType="inquirybook.InquiryBookAttVo" parameterType="int">
		SELECT * FROM inquiry_photo WHERE pserial = #{_parameter}
	</select>
	
	<delete id="delete" parameterType="inquirybook.InquiryBookVo">
		DELETE FROM inquiry WHERE serial = #{serial} and pwd = #{pwd}
	</delete>
	<delete id="delete_att_pserial" parameterType="int">
		DELETE FROM inquiry_photo WHERE pserial = #{_parameter}
	</delete>
	<update id="update" parameterType="inquirybook.InquiryBookVo">
		UPDATE inquiry set subject = #{subject}, doc = #{doc} WHERE serial = #{serial} and pwd = #{pwd}
	</update>
	
	
	<delete id="delete_att" parameterType="inquirybook.InquiryBookVo">
		DELETE FROM inquiry_photo
		<where>
			<foreach collection="delFiles" item="att" separator="or">
				sysfile = #{att.sysFile}
			</foreach>		

		</where>	
	</delete>
</mapper>   